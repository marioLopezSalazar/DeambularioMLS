<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/card_faq"
    style="@style/Card_teacher"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:clickable="true"
    android:focusable="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <TextView
            android:id="@+id/text_faq_question"
            style="@style/Subtitle_teacher"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:textColor="?attr/colorOnSurface"
            android:textStyle="bold"
            app:drawableEndCompat="@drawable/icon_expand"
            app:drawableTint="?attr/colorPrimary"
            tools:text="How can I create a new route session?" />

        <TextView
            android:id="@+id/text_faq_answer"
            style="@style/Text_teacher"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:textColor="?attr/colorOutlineVariant"
            android:visibility="gone"
            tools:text="On the routes list, click on the left-hand side button. Note that routes with no milestones cannot be played."
            tools:visibility="visible" />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>